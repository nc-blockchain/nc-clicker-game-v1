{
  "name": "nc-clicker-game-v1",
  "version": "1.0.1",
  "description": "1N Blockchain Token Clicker - $NCCC (post-migration; faucet pays NCCC on Base)",
  "type": "module",
  "scripts": {
    "build:wallet": "node build-wallet.js",
    "build": "npm run build:wallet",
    "serve": "npx http-server . -p 3001 -c-1",
    "start": "npx http-server . -p 3001 -c-1",
    "open": "node serve-and-open.cjs",
    "test:e2e": "playwright test",
    "db:link": "supabase link --project-ref kjzuyncvahefckizbigb",
    "db:push": "supabase db push",
    "db:migrate:017": "node scripts/run-migration-017.cjs"
  },
  "dependencies": {
    "@tonconnect/sdk": "^2.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "esbuild": "^0.24.0"
  }
}

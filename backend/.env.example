# Server (3002 to avoid conflict with other apps on 3000)
PORT=3002

# TON: admin wallet (minter admin). Only this wallet can mint $NCCC.
# Use a dedicated wallet for the game; never use your main wallet.
ADMIN_MNEMONIC=word1 word2 ... word24

# $NCCC Jetton minter contract address (after you deploy)
# (Env var name keeps the CARD_ prefix for backward compatibility with existing deploys.)
TON_CARD_MINTER_ADDRESS=EQ...

# Network: testnet or mainnet
TON_NETWORK=testnet

# Optional: protect admin mint endpoint
ADMIN_API_KEY=your-secret-api-key
